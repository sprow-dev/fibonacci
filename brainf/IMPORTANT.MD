# IMPORTANT!!!

**This code is completely vibe-coded.** The only thing I did was strip the AI's code comments.
However, I told the AI ideas and it simply made the code implementation.
Here are my contributions:
- I was the one to make the scuffed build stack to make it run the code at insane speeds
- I was the one to come up with the optimizer and converter stack
- I was the one to come up with the logic for how the optimizer optimizes code
- I was the one to have it buffer writes
- The main.c code is based on my own human-written code

Here are the AI's contributions:
- It made the converter stack. I do not understand brainf very well so it had to make that
- It made all the code implemtations of my ideas
- It made the makefile
- It wrote the brainf code itself.
- It came up with a single (1) optimization itself for optimizer.c.
